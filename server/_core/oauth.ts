import type { Express } from "express";

export function registerOAuthRoutes(app: Express) {
  // Les routes OAuth de Manus sont désactivées pour l'autonomie du projet.
  // Vous pouvez implémenter vos propres routes de login local ici si nécessaire.
  console.log("[Auth] Manus OAuth routes disabled.");
}
