name: DB Migrate (safe) & Baseline

on:
  workflow_dispatch:

jobs:
  migrate:
    runs-on: ubuntu-latest
    env:
      DATABASE_URL: ${{ secrets.DATABASE_URL }}

    steps:
      - uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: "pnpm"

      - name: Install deps
        run: pnpm install

      - name: Debug DATABASE_URL presence (masked)
        run: |
          if [ -z "$DATABASE_URL" ]; then
            echo "DATABASE_URL is EMPTY"
            exit 1
          fi
          echo "DATABASE_URL is set (length=${#DATABASE_URL})"
          echo "$DATABASE_URL" | sed -E 's#(mysqls?://[^:]+:)[^@]+@#\1****@#'

      - name: Rename legacy table processes -> processus (idempotent)
        run: |
          node - <<'NODE'
          const mysql = require("mysql2/promise");
          (async () => {
            const conn = await mysql.createConnection(process.env.DATABASE_URL);
            const [a] = await conn.query(`SHOW TABLES LIKE 'processes'`);
            const [b] = await conn.query(`SHOW TABLES LIKE 'processus'`);
            if (a.length === 0) { console.log("No rename needed for processes ✅"); await conn.end(); return; }
            if (b.length > 0) { console.log("Table 'processus' already exists, skipping rename ✅"); await conn.end(); return; }
            console.log("Renaming table processes -> processus ...");
            await conn.query(`RENAME TABLE \`processes\` TO \`processus\``);
            console.log("Rename done ✅");
            await conn.end();
          })().catch((e) => { console.error(e); process.exit(1); });
          NODE

      # ✅ Make journal + run migrate in the SAME step (cannot be skipped)
      - name: Ensure Drizzle journal exists then migrate (single-step, safe)
        run: |
          set -euxo pipefail

          # Create both possible journal locations
          mkdir -p meta
          mkdir -p drizzle/meta
          mkdir -p drizzle/migrations

          if [ ! -f meta/_journal.json ]; then
            echo '{"version":"5","dialect":"mysql","entries":[]}' > meta/_journal.json
          fi

          if [ ! -f drizzle/meta/_journal.json ]; then
            echo '{"version":"5","dialect":"mysql","entries":[]}' > drizzle/meta/_journal.json
          fi

          echo "== meta =="
          ls -la meta
          cat meta/_journal.json

          echo "== drizzle/meta =="
          ls -la drizzle/meta
          cat drizzle/meta/_journal.json

          echo "== drizzle/migrations =="
          ls -la drizzle/migrations || true

          pnpm exec drizzle-kit migrate --config=drizzle.config.ts
